# CareerPilot 开发文档

> 项目定位：基于证据链的求职准备与面试训练智能助理  
> 技术路线：Python + FastAPI + LangGraph + React + PostgreSQL + Qdrant  
> 文档状态：V1.0，作为需求、架构、实现与验收的统一基线  
> 适用阶段：课程/竞赛作品、个人简历项目、可继续演进的真实应用

---

## 1. 文档目的

本文档回答以下问题：

1. 项目究竟解决什么问题，服务哪些用户。
2. 第一版必须开发哪些功能，哪些功能暂时不开发。
3. Skill、MCP、RAG、Agent Harness 如何在同一套系统中协作。
4. 前端、后端、数据库、向量检索、模型服务如何分层。
5. 为什么选择 React、FastAPI、LangGraph、Qdrant 等技术。
6. 为什么第一版不选择 Milvus、pgvector、Elasticsearch、微服务和多 Agent 群。
7. 数据模型、接口、测试、评测、安全和部署如何设计。
8. 最终如何形成一个可以运行、演示、量化和写进简历的工程项目。

本文档是开发基线。后续如果调整技术或需求，需要记录变更原因，而不是直接修改实现导致文档与代码脱节。

---

## 2. 项目概述

### 2.1 项目名称

中文名称：职途——求职准备与面试训练智能助理  
英文名称：CareerPilot  

名称不是核心，仓库和代码中统一使用 career-pilot。

### 2.2 项目背景

传统求职类 AI 产品常见问题包括：

- 只会润色简历，容易编造用户没有做过的技术和指标。
- 只会根据题库随机提问，不能结合目标 JD 和用户真实项目。
- 每次对话相互独立，无法持续追踪薄弱知识点。
- 学习计划是静态文本，无法根据练习结果动态调整。
- 回答没有引用依据，用户无法判断内容来自简历、项目资料还是模型猜测。
- 外部工具调用缺少审批，可能重复创建日历事件或误发邮件。
- Agent 执行过程不可观测，失败后难以定位原因。

CareerPilot 的核心不是“生成更多内容”，而是建立一个可追溯的求职准备闭环：

    目标岗位
       ↓
    JD 能力抽取
       ↓
    简历与项目证据匹配
       ↓
    能力差距与学习计划
       ↓
    自适应模拟面试
       ↓
    评分、复盘与薄弱点记忆
       ↓
    动态调整下一轮计划

### 2.3 核心价值

项目需要始终围绕三个价值判断功能是否值得开发：

1. 可信：关键结论应尽量引用用户资料，不编造经历。
2. 可执行：分析结果需要转化为学习任务、模拟面试和后续行动。
3. 可改进：系统要记住用户长期薄弱点，并用评测数据证明系统效果。

### 2.4 目标用户

主要用户：

- 准备校招或社招的软件开发者。
- 重点支持 Java、Python、前端和 AI 应用开发岗位。
- 需要根据不同 JD 调整简历和学习计划的求职者。
- 希望练习项目深挖、技术面试和行为面试的用户。

第一版不做招聘方 ATS，不做企业人才筛选，不对候选人做自动录用决策。

### 2.5 典型用户故事

用户上传简历、项目 README 和三个 Java 后端 JD，然后说：

> 我 14 天后参加 Java 后端面试，每天晚上能学习两个小时。请分析我和这些岗位的差距，安排备战计划，并从今天开始模拟面试。

系统应当：

1. 检查是否缺少面试日期、可用时间、目标优先级等信息。
2. 从 JD 中抽取岗位能力。
3. 从简历和项目资料中检索真实证据。
4. 输出岗位要求、个人证据、覆盖程度、缺口和建议。
5. 生成两周学习计划。
6. 经用户确认后创建日历事件。
7. 根据薄弱点开始自适应模拟面试。
8. 面试结束后生成带证据的评分报告。
9. 更新用户能力画像并调整后续计划。

---

## 3. 项目范围

### 3.1 P0：必须完成的核心功能

P0 功能决定项目是否完整，必须优先开发。

#### 3.1.1 用户与工作空间

- 本地账户注册与登录。
- 创建多个求职工作空间。
- 工作空间之间的数据隔离。
- 配置目标岗位、面试日期、每日可用时间。
- 配置模型提供商和 API Key，密钥只在后端加密保存。

#### 3.1.2 文档知识库

- 上传 PDF、DOCX、TXT、Markdown。
- 录入或粘贴 JD。
- 展示解析状态、索引状态和失败原因。
- 查看文档内容、页码、段落和切分结果。
- 删除单个文档及其索引。
- 对相同文件进行哈希去重。
- 文档更新后创建新版本并重新索引。

#### 3.1.3 带引用的知识问答

- 针对指定工作空间进行问答。
- 支持多轮对话和流式输出。
- 回答展示引用片段、文档名称和页码。
- 用户可以点击引用查看原文上下文。
- 检索不到证据时明确说明，不允许伪造引用。

#### 3.1.4 JD 能力差距分析

- 从 JD 抽取技能项、经验要求和软技能。
- 识别必须项、加分项和岗位职责。
- 从简历、项目文档中寻找对应证据。
- 输出覆盖、部分覆盖、未覆盖和证据冲突。
- 生成优先级排序和补强建议。
- 支持比较多个 JD 的共同要求与差异。

#### 3.1.5 自适应模拟面试

- 支持 Java/Python/前端/AI 应用等面试类型。
- 支持基础知识、项目深挖、系统设计、HR 行为面试。
- 每次只提出一个问题。
- 根据用户回答进行追问或进入下一题。
- 面试过程中不提前泄露评分答案。
- 面试结束后统一评分和复盘。
- 评分必须包含评分标准、证据、优点、问题和改进建议。

#### 3.1.6 个性化记忆

- 保存用户明确确认的目标、偏好和时间安排。
- 保存各知识点掌握程度和最近练习结果。
- 保存历史面试中的高频错误。
- 用户可以查看、修改、删除单条记忆。
- 对低置信度推断先征求确认，再写入长期记忆。

#### 3.1.7 学习计划

- 根据面试日期、时间预算和能力差距生成计划。
- 支持查看日、周任务。
- 支持完成、延期、跳过和重新安排。
- 根据模拟面试结果调整任务优先级。
- 计划变更保留历史版本。

#### 3.1.8 Agent 执行轨迹

- 展示当前 Agent 所在步骤。
- 展示加载的 Skill。
- 展示检索到的文档及分数。
- 展示调用的工具、耗时和结果摘要。
- 展示需要用户批准的操作。
- 展示 Token、模型、总耗时和错误信息。

### 3.2 P1：形成技术差异化的增强功能

- Skill 注册、版本管理和动态加载。
- MCP 客户端接入 GitHub、日历和只读文件服务。
- 创建日历事件前的 Human-in-the-loop 审批。
- GitHub 项目事实抽取和简历一致性检查。
- RAG 离线评测和方案对比页面。
- Agent 运行回放与失败重试。
- Prompt、Skill、模型和检索配置的版本记录。
- 将内部核心能力暴露为 Career MCP Server。

### 3.3 P2：时间充足时再开发

- 语音输入和语音面试。
- 多模态简历截图或手写笔记识别。
- 面向好友或导师的报告分享。
- 企业岗位数据批量导入。
- 在线部署、多租户和配额计费。
- 端侧或完全离线大模型模式。

### 3.4 明确不做

第一版不开发以下内容：

- 多 Agent 自由讨论或 Agent 群聊。
- GraphRAG 和图数据库。
- Kubernetes、服务网格和分布式微服务。
- 自研向量数据库。
- 数字人和视频面试官。
- 自动海投、自动发送邮件或未经确认的日历写入。
- 同时维护多个向量数据库。
- 没有基准数据支撑的自动反思循环。

这些功能不是永远不能做，而是当前没有足够收益支撑其复杂度。

---

## 4. 总体架构

### 4.1 架构原则

1. 模块化单体优先：后端一个代码库和一个 API 服务，业务模块清晰隔离。
2. Python 优先：业务后端、Agent、RAG、文档处理和评测全部使用 Python。
3. 前后端分离：React SPA 负责交互，FastAPI 提供 API 和流式事件。
4. 长任务异步化：文档索引、批量评测等任务交给 Celery Worker。
5. 确定性优先：能用普通代码解决的逻辑，不交给大模型自由判断。
6. 写操作审批：外部系统写入和高风险工具调用必须由用户确认。
7. 可替换边界：模型、向量数据库、Embedding 和 MCP Server 通过接口适配。
8. 可观测：每次 Agent 运行都应留下可检索的结构化轨迹。

### 4.2 逻辑架构图

    ┌─────────────────────────────────────────────────────────────┐
    │                       React Web SPA                         │
    │ 对话 / 知识库 / JD 分析 / 模拟面试 / 学习计划 / 运行轨迹   │
    └───────────────────────┬─────────────────────────────────────┘
                            │ REST + SSE
    ┌───────────────────────▼─────────────────────────────────────┐
    │                       FastAPI API                           │
    │ Auth / Workspace / Document / Interview / Plan / Admin     │
    ├─────────────────────────────────────────────────────────────┤
    │                     Agent Harness                           │
    │ Router / Context / Skill / Tool / Policy / Checkpoint      │
    │ Model Gateway / Memory / Trace / Evaluation                │
    ├─────────────────────────────────────────────────────────────┤
    │                    LangGraph Workflows                      │
    │ QA / JD Gap / Mock Interview / Study Plan / Approval       │
    ├───────────────────────┬───────────────────────┬─────────────┤
    │ RAG Service           │ MCP Client Manager    │ Domain      │
    │ Parse/Retrieve/Rerank │ GitHub/Calendar/File  │ Services    │
    └──────────┬────────────┴───────────┬───────────┴──────┬──────┘
               │                        │                  │
        ┌──────▼──────┐          ┌──────▼──────┐   ┌──────▼──────┐
        │   Qdrant    │          │ MCP Servers │   │ PostgreSQL  │
        │ Vector/Sparse│         │ External Apps│   │ Source Data │
        └─────────────┘          └─────────────┘   └──────┬──────┘
                                                         │
                     ┌─────────────┐              ┌──────▼──────┐
                     │ Redis       │              │ MinIO       │
                     │ Queue/Cache │              │ Raw Files   │
                     └──────┬──────┘              └─────────────┘
                            │
                     ┌──────▼──────┐
                     │Celery Worker│
                     │Ingest/Evals │
                     └─────────────┘

### 4.3 进程与部署单元

本地 Docker Compose 包含：

| 服务 | 职责 | 是否必需 |
|---|---|---:|
| web | React 构建产物和前端开发服务 | 是 |
| api | FastAPI、Agent Harness、业务接口 | 是 |
| worker | 文档解析、Embedding、批量评测 | 是 |
| postgres | 业务数据、对话、Checkpoint、审计 | 是 |
| redis | Celery Broker、短期缓存、SSE 事件协调 | 是 |
| qdrant | Dense/Sparse 向量与元数据过滤 | 是 |
| minio | 原始文件和导出报告 | 是 |
| reverse-proxy | 统一入口和静态资源，可使用 Nginx/Caddy | 生产建议 |
| langfuse | LLM 可观测平台 | 可选 |

第一版不把 Agent、RAG、Skill、MCP 拆成独立网络服务。它们是 FastAPI 内部模块，在性能或团队规模确有需要时再拆分。

### 4.4 一次完整请求的数据流

以“结合 JD 和简历生成两周计划”为例：

1. React 调用 POST /api/v1/agent/runs 创建运行。
2. FastAPI 返回 run_id，React 订阅 SSE 事件。
3. Harness 创建 LangGraph State 和持久化 Checkpoint。
4. Router 识别任务类型为 jd_gap_and_plan。
5. Skill Registry 加载 JD 分析和学习计划 Skill。
6. Context Builder 读取工作空间、用户画像和最近对话。
7. RAG 检索 JD、简历和项目证据。
8. 模型输出结构化能力矩阵，Pydantic 进行校验。
9. 计划节点根据时间预算生成任务。
10. 如需写入日历，Policy Engine 创建 approval，图执行暂停。
11. React 展示待审批动作。
12. 用户批准后，Harness 恢复同一 thread_id。
13. MCP Client 调用日历工具，并使用幂等键避免重复创建。
14. Harness 保存最终结果、运行轨迹、Token 和耗时。
15. Memory Manager 提取候选记忆；需确认的内容不直接写入。

---

## 5. Agent 整体架构

### 5.1 Agent 设计原则

本项目不采用“一个无限循环的自主 Agent”，而采用“受控状态图 + 专项工作流”：

- Router 负责识别任务，不负责完成所有任务。
- 每类复杂任务对应一个 LangGraph Subgraph。
- Skill 提供业务 SOP、工具白名单和输出契约。
- Tool 执行由 Harness 控制，而不是由模型直接运行任意代码。
- 对高风险动作使用 Interrupt 暂停和恢复。
- 每个节点尽量幂等，避免恢复执行时产生重复副作用。

### 5.2 Agent Harness 组成

#### 5.2.1 Run Manager

职责：

- 创建 run_id、thread_id 和 trace_id。
- 保存运行状态：queued、running、waiting_approval、completed、failed、cancelled。
- 控制最大步骤数、总超时和最大 Token。
- 将步骤事件推送给前端。

#### 5.2.2 Model Gateway

职责：

- 屏蔽不同模型供应商差异。
- 支持聊天模型、Embedding 模型、Reranker 三类能力。
- 支持结构化输出。
- 记录 Token、费用、延迟和错误。
- 按任务选择模型。
- 实现超时、重试、限流和熔断。

模型任务分级：

| 任务 | 推荐模型策略 |
|---|---|
| 意图识别、标签抽取 | 小模型或规则 |
| JD 结构化抽取 | 支持结构化输出的中小模型 |
| 复杂项目追问 | 较强推理模型 |
| 最终面试评分 | 与提问模型隔离的强模型调用 |
| Embedding | BGE-M3 或可替换的中文 Embedding |
| Rerank | BGE Reranker 系列或 API Reranker |

第一版通过 OpenAI-compatible 接口接入生成模型，避免业务代码绑定某一家供应商。

#### 5.2.3 Context Builder

职责：

- 组合系统规则、Skill 指令、最近对话、RAG 证据、记忆和工具结果。
- 控制上下文 Token 预算。
- 删除低相关和重复片段。
- 对长对话生成滚动摘要。
- 明确区分可信系统指令与不可信外部文档。

建议上下文预算：

| 内容 | 预算比例 |
|---|---:|
| 系统与安全规则 | 15% |
| 当前 Skill | 15% |
| 最近对话与任务状态 | 20% |
| RAG 证据 | 35% |
| 工具结果与输出空间 | 15% |

比例仅作为初始值，后续通过评测调整。

#### 5.2.4 Skill Registry

职责：

- 扫描并注册 Skill。
- 根据名称、版本、触发条件加载 Skill。
- 校验 Skill Manifest。
- 管理允许调用的工具。
- 记录运行时实际使用的 Skill 版本。

#### 5.2.5 Tool Registry

工具来源分为：

1. 内部工具：RAG 检索、数据库查询、记忆读取等 Python 函数。
2. MCP 工具：GitHub、日历、只读文件等外部能力。
3. 系统工具：时间、格式化、计算等确定性工具。

所有工具必须具有：

- 唯一名称。
- Pydantic 输入输出模型。
- 超时。
- 风险等级。
- 是否幂等。
- 是否需要审批。
- 允许使用该工具的 Skill 列表。

#### 5.2.6 Policy Engine

职责：

- 判断工具调用是否允许。
- 检查当前 Skill 是否拥有工具权限。
- 对外部写操作创建审批。
- 限制单次运行最大工具调用次数。
- 阻止访问工作空间外的数据。
- 检测 Prompt Injection 和敏感信息外泄风险。

工具风险等级：

| 等级 | 示例 | 策略 |
|---|---|---|
| R0 只读 | 检索简历、读取 JD | 自动执行 |
| R1 本地低风险写入 | 保存草稿、创建内部任务 | 自动执行并审计 |
| R2 外部写入 | 创建日历、修改远程任务 | 必须确认 |
| R3 高风险 | 删除远程数据、发送正式邮件 | 二次确认或第一版禁用 |

#### 5.2.7 Checkpoint Store

使用 LangGraph Postgres Checkpointer 保存图状态。

需要支持：

- 多轮对话恢复。
- 模拟面试中断后继续。
- 等待用户审批。
- 节点失败后从最近 Checkpoint 恢复。
- 在调试环境回放历史执行。

LangGraph 官方持久化能力支持 checkpoint、memory、human-in-the-loop、状态历史和故障恢复，适合本项目的长任务场景。

#### 5.2.8 Memory Manager

负责读取、生成、确认、写入和衰减长期记忆，详见第 9 章。

#### 5.2.9 Trace Manager

每个步骤记录：

- run_id、thread_id、step_id、parent_step_id。
- 节点名称和状态。
- 输入输出摘要。
- 模型和 Prompt 版本。
- Skill 版本。
- 工具调用及风险等级。
- RAG 查询、候选结果和最终上下文。
- Token、费用、耗时和重试次数。
- 错误类型和堆栈摘要。

敏感内容默认脱敏，不在前端展示 API Key 或完整隐私文本。

### 5.3 LangGraph 状态模型

建议状态结构：

    AgentState
      run_id
      thread_id
      workspace_id
      user_id
      messages
      user_intent
      active_skill
      task_input
      missing_fields
      retrieved_evidence
      tool_requests
      tool_results
      approval_id
      structured_result
      memory_candidates
      errors
      step_count
      token_usage
      final_answer

状态中只保存可序列化数据。数据库连接、模型客户端和函数对象通过依赖注入获得，不写入 Checkpoint。

### 5.4 主图节点

    START
      ↓
    authenticate_and_scope
      ↓
    input_guard
      ↓
    classify_intent
      ↓
    select_skill
      ↓
    collect_missing_information ──需要补充──→ WAIT_USER
      ↓
    dispatch_subgraph
      ↓
    validate_result
      ↓
    memory_candidate_extraction
      ↓
    render_response
      ↓
    END

### 5.5 专项 Subgraph

#### 5.5.1 Knowledge QA

    rewrite_query
      → retrieve
      → rerank
      → check_evidence
      → answer_with_citations
      → citation_validation

#### 5.5.2 JD Gap Analysis

    parse_jd
      → normalize_competencies
      → retrieve_resume_evidence
      → verify_evidence
      → calculate_coverage
      → prioritize_gaps
      → generate_report

#### 5.5.3 Mock Interview

    initialize_session
      → choose_topic
      → ask_one_question
      → WAIT_USER_ANSWER
      → analyze_answer
      → decide_follow_up
      → follow_up_or_next
      → final_evaluation
      → memory_candidates

面试提问节点和最终评分节点使用不同 Prompt；条件允许时使用不同模型调用，降低自评偏差。

#### 5.5.4 Study Plan

    load_goal_and_availability
      → load_gap_priorities
      → allocate_time
      → generate_tasks
      → detect_conflicts
      → ask_for_approval
      → optional_calendar_write
      → persist_plan

### 5.6 失败处理

错误分为：

- 可重试：网络超时、模型限流、临时 MCP 失败。
- 可降级：Reranker 不可用时返回融合检索结果。
- 需要用户处理：文件损坏、权限不足、API Key 无效。
- 不可恢复：数据约束破坏、状态版本不兼容。

每个节点定义最大重试次数。对具有副作用的节点，重试前必须检查幂等键和已有执行结果。

---

## 6. Skill 架构

### 6.1 Skill 定位

Skill 是可版本化、可测试、可约束的领域工作流说明，不等同于一段普通 Prompt。

Skill 负责定义：

- 什么时候使用。
- 完成任务的步骤。
- 缺少哪些输入时必须追问。
- 可以调用哪些工具。
- 输出必须符合什么结构。
- 哪些行为禁止执行。
- 如何评测该 Skill 是否有效。

### 6.2 第一版 Skill 列表

| Skill | 主要能力 | P0/P1 |
|---|---|---:|
| grounded-qa | 基于上传资料回答并生成引用 | P0 |
| jd-gap-analysis | JD 与个人证据差距分析 | P0 |
| mock-interview | 自适应模拟面试 | P0 |
| project-deep-dive | 基于项目资料进行深挖 | P0 |
| study-plan | 生成并动态调整学习计划 | P0 |
| answer-review | 对单次面试回答进行评分 | P0 |
| resume-fact-check | 检查简历描述是否有证据 | P1 |
| weekly-review | 汇总一周进度并调整目标 | P1 |
| calendar-sync | 审批后同步学习计划 | P1 |
| github-evidence | 从代码仓库提取项目证据 | P1 |

### 6.3 Skill 文件结构

    backend/app/skills/
      grounded-qa/
        SKILL.md
        manifest.yaml
        output.schema.json
        examples/
        evals/
      jd-gap-analysis/
      mock-interview/
      study-plan/

manifest.yaml 包含：

    name: jd-gap-analysis
    version: 1.0.0
    description: 分析岗位要求与用户证据之间的差距
    triggers:
      - 分析这个岗位
      - 对比简历和JD
      - 我还缺少哪些能力
    required_inputs:
      - job_description_ids
    optional_inputs:
      - resume_document_ids
      - target_interview_date
    allowed_tools:
      - rag.search
      - document.get_metadata
      - memory.read_competency_profile
    output_schema: output.schema.json
    risk_level: R0
    eval_dataset: evals/cases.jsonl

### 6.4 Skill 选择策略

第一版采用规则与模型组合：

1. 显式命令或前端入口优先，例如用户点击“开始模拟面试”。
2. 关键词和当前页面上下文其次。
3. 无法确定时调用小模型分类。
4. 置信度低于阈值时向用户确认，不自动选择高风险 Skill。

不使用纯向量相似度决定 Skill，因为容易在相似任务之间误路由。

### 6.5 Skill 测试

每个 Skill 至少包含：

- 5 个正常用例。
- 3 个缺少信息用例。
- 3 个恶意或越权用例。
- 2 个输出格式失败用例。
- 1 个工具故障用例。

指标包括：

- 路由准确率。
- 输出 Schema 通过率。
- 工具选择准确率。
- 引用正确率。
- 任务完成率。
- 平均 Token 和延迟。

---

## 7. MCP 架构

### 7.1 MCP 在本项目中的边界

MCP 用于连接具有独立边界的外部数据或外部操作，不用于包装同一进程中的所有 Python 函数。

适合 MCP：

- GitHub 仓库、提交和 Issue。
- Google/Outlook 日历。
- 用户授权的本地文件资源。
- 搜索服务。
- 将 CareerPilot 能力提供给外部 MCP 客户端。

不适合 MCP：

- FastAPI 内部查询 PostgreSQL。
- 调用同一模块中的 RAG Service。
- 简单日期计算和字符串格式化。

内部调用直接使用 Python 接口，更简单、更易测试。

### 7.2 MCP Client Manager

职责：

- 注册多个 MCP Server。
- 获取工具、资源和 Prompt 清单。
- 将 MCP Schema 转换为内部 Tool Definition。
- 管理连接生命周期、超时和重连。
- 将工作空间凭证安全注入指定 Server。
- 记录每次调用和返回摘要。
- 在调用前经过 Policy Engine。

### 7.3 第一批 MCP 能力

#### 7.3.1 GitHub

只读工具：

- 获取仓库信息和 README。
- 列出目录和读取文件。
- 搜索代码。
- 获取提交记录、Issue 和 Pull Request。

应用：

- 验证简历中的项目技术是否真实存在。
- 根据真实代码生成项目深挖题。
- 从提交记录中提取用户实际贡献。
- 发现 README 与实现不一致的内容。

第一版不允许 Agent 自动修改仓库、提交代码或创建 Pull Request。

#### 7.3.2 Calendar

只读：

- 查询指定日期范围内事件。
- 查询空闲时间。

写入：

- 创建学习任务。
- 更新经用户确认的学习日程。

所有写操作为 R2，必须审批，并传入 idempotency_key。

#### 7.3.3 Filesystem

- 限定工作空间授权目录。
- 默认只读。
- 拒绝路径穿越。
- 不允许读取密钥文件和系统目录。
- 大文件只返回元数据或分页内容。

### 7.4 Career MCP Server

P1 阶段将本项目核心能力暴露为 MCP Server：

| Tool | 作用 |
|---|---|
| analyze_job_description | 解析 JD 并返回能力要求 |
| search_resume_evidence | 检索简历和项目证据 |
| create_study_plan | 生成学习计划草案 |
| start_mock_interview | 创建模拟面试会话 |
| evaluate_answer | 评估单次回答 |
| get_weakness_report | 获取薄弱点报告 |

Resources 可提供：

- 当前工作空间的文档列表。
- 已授权的能力画像。
- 学习计划摘要。
- 模拟面试报告。

对外 MCP Server 默认只暴露只读能力；涉及个人数据必须要求用户授权。

### 7.5 MCP 安全要求

- 每个 Server 使用独立凭证。
- 凭证后端加密存储，不进入 Prompt。
- 工具输入使用 Pydantic/JSON Schema 校验。
- 工具输出视为不可信外部数据，不能变成系统指令。
- 限制返回体大小，防止上下文注入。
- 记录 server、tool、参数摘要、耗时、状态和审批记录。
- 外部写操作必须采用幂等键。
- Server 不可用时只降级相关功能，不能导致整个对话崩溃。

---

## 8. RAG 架构

### 8.1 RAG 目标

RAG 主要解决：

- 模型不知道用户的简历、项目和历史表现。
- 模型容易编造经历或性能指标。
- 用户需要看到结论依据。
- 文档更新后不适合重新训练模型。
- 多个用户和工作空间需要严格隔离。

RAG 不负责执行外部操作，也不负责替代业务数据库。

### 8.2 文档类型

| 类型 | 示例 | 关键元数据 |
|---|---|---|
| resume | 简历 PDF/DOCX | 版本、页码、章节 |
| job_description | JD 文本/PDF | 公司、岗位、来源 |
| project_doc | README、设计文档 | 项目、模块、文件路径 |
| study_note | Markdown、课程笔记 | 技术主题、章节 |
| interview_record | 历史问答和报告 | 场次、题目、评分 |
| code_evidence | GitHub 文件摘要 | 仓库、commit、路径 |

### 8.3 Ingestion Pipeline

    upload
      → MIME 与大小校验
      → SHA-256 去重
      → MinIO 保存原始文件
      → 创建 document_version
      → Celery 异步解析
      → 清洗与结构识别
      → Chunk 切分
      → Dense/Sparse 向量生成
      → 写入 Qdrant
      → 抽样校验
      → 更新 indexing_status

索引状态：

- pending
- parsing
- chunking
- embedding
- indexing
- ready
- failed

失败任务记录当前阶段和可读错误，允许从失败阶段重试。

### 8.4 文档解析

默认解析器：

| 格式 | 技术 |
|---|---|
| PDF | PyMuPDF |
| DOCX | python-docx |
| Markdown | markdown-it-py |
| TXT | 标准库 |
| HTML | BeautifulSoup，仅处理用户明确导入内容 |

扫描 PDF 的 OCR 作为 P1，可接入 PaddleOCR 或独立解析服务。第一版不因为少量扫描件引入复杂文档平台。

解析结果必须保留：

- 页码。
- 标题层级。
- 段落序号。
- 字符偏移。
- 原始文件版本。
- 表格和列表的基本结构。

### 8.5 Chunk 策略

不使用固定字符数直接硬切全部文档。

建议：

1. 先按标题、段落、列表和表格切分。
2. 小段落合并到目标 Token 范围。
3. 超长段落按句子切分，并保留少量重叠。
4. 简历按模块切分，避免将项目名和项目描述分离。
5. JD 按职责、要求、加分项切分。
6. 代码按文件、类、函数切分，不直接按普通文本处理。

初始参数：

- 目标 300 至 500 Tokens。
- 重叠 50 至 80 Tokens。
- 引用展示时补充前后相邻 Chunk。

参数必须通过评测确定，不能把初始值当成最佳值。

### 8.6 Embedding 与 Sparse 表示

推荐默认：

- Dense Embedding：BGE-M3，兼顾中文、英文和技术文本。
- Sparse：优先使用同一模型产生的 Sparse Lexical Weights；开发初期可先使用 Qdrant/FastEmbed 支持的 BM25 Sparse。
- Reranker：BGE Reranker v2 M3 或可配置 API。

为了降低开发门槛，模型通过接口配置：

    EmbeddingProvider
      embed_documents(texts)
      embed_query(text)
      dimensions
      model_version

    SparseProvider
      encode_documents(texts)
      encode_query(text)

    RerankProvider
      rerank(query, candidates)

如果开发设备不适合本地运行 BGE-M3，可以使用远程 Embedding API，但必须记录模型名称、维度和版本。

### 8.7 Qdrant Collection 设计

第一版使用一个 chunks Collection，通过 workspace_id 和 document_type 过滤。

向量字段：

- dense：语义向量。
- sparse：关键词或 Sparse Lexical 向量。

Payload：

- chunk_id
- workspace_id
- document_id
- document_version_id
- document_type
- title_path
- page_number
- project_id
- language
- created_at
- embedding_version
- is_active

原始文件保存在 MinIO，结构化文档和 Chunk 文本以 PostgreSQL 为事实来源；Qdrant 保存检索所需向量和必要 Payload。这样重建索引时不依赖 Qdrant 作为唯一数据源。

### 8.8 Query Pipeline

    user query
      → 识别查询类型
      → 可选查询改写
      → workspace/document metadata filter
      → Dense Top 20
      → Sparse Top 20
      → RRF 融合
      → 去重
      → Reranker Top 8
      → 证据阈值判断
      → Context Builder 选择 Top 4 至 6
      → 带引用生成
      → 引用校验

查询改写只在以下情况启用：

- 用户使用代词依赖上下文。
- 查询过短且意图不明确。
- 需要将“这个项目”解析成具体项目。

不默认生成多个查询进行大规模召回，避免无意义增加成本。

### 8.9 引用设计

模型上下文中的每个片段带稳定 citation_id：

    source_1
      document_id
      document_version_id
      chunk_id
      file_name
      page_number
      text

最终回答使用 source_1 等引用 ID，后端再转换为前端引用对象。

引用校验：

- 引用 ID 必须存在于本次上下文。
- 引用原文必须属于当前用户和工作空间。
- 关键事实至少有一个支持片段。
- 没有证据时输出“未在已上传资料中找到依据”。
- 不允许模型自己生成文件名和页码。

### 8.10 RAG 评测

建立人工标注数据集：

- 50 个简历事实问题。
- 50 个 JD 能力问题。
- 30 个跨文档比较问题。
- 20 个应当拒答或说明无证据的问题。

指标：

- Recall@5、Recall@10。
- MRR。
- Context Precision。
- Context Recall。
- Faithfulness。
- Citation Precision。
- Citation Coverage。
- 无证据问题的正确拒答率。
- 检索 P50/P95 延迟。
- 每次查询成本。

至少对比：

1. Dense Only。
2. Sparse Only。
3. Dense + Sparse + RRF。
4. Dense + Sparse + RRF + Reranker。

最终根据评测结果选择默认参数，而不是根据框架示例决定。

---

## 9. 记忆架构

### 9.1 为什么记忆不能等于聊天记录向量化

如果把所有对话直接写入向量库，容易出现：

- 用户随口说的错误信息变成长期事实。
- 旧目标覆盖新目标。
- 同一能力出现相互矛盾的判断。
- 低价值闲聊污染检索。
- 用户无法理解系统究竟记住了什么。

因此本项目将记忆拆成明确类型，并让 Harness 控制何时读写。

### 9.2 记忆类型

#### 9.2.1 Profile Memory

稳定、结构化的用户信息：

- 目标岗位。
- 偏好语言。
- 每日可用时间。
- 面试日期。
- 希望的面试风格。

此类记忆写入 PostgreSQL，不需要向量化。

#### 9.2.2 Competency Memory

用户能力画像：

- 技能名称。
- 当前掌握分数。
- 评分置信度。
- 最近评估时间。
- 支撑的面试记录。
- 变化趋势。

能力分数不能只由一次 LLM 评分决定。初始建议采用加权更新：

    new_score =
      old_score × 0.7
      + latest_assessment × 0.3

权重后续根据实验调整，并保留每次变化记录。

#### 9.2.3 Episodic Memory

有时间和上下文的事件：

- 某次模拟面试。
- 某道题的典型错误。
- 某个学习任务的完成情况。
- 用户对某项建议的明确反馈。

事件保存在 PostgreSQL；需要语义召回时，将摘要写入 Qdrant 的独立 memory Collection。

#### 9.2.4 Conversation Memory

- 最近 N 轮完整消息。
- 更早对话的滚动摘要。
- LangGraph 当前任务 State。

Checkpoint 与长期记忆必须区分：Checkpoint 用于恢复任务，Memory 用于跨任务个性化。

### 9.3 写入策略

候选记忆产生后分为：

- 明确事实：用户直接说明，可自动写入。
- 系统评估：保留证据和置信度。
- 模型推断：必须用户确认。
- 敏感信息：默认不写入。

每条记忆包含：

- source_type
- source_id
- confidence
- created_at
- last_used_at
- expires_at
- user_confirmed
- status

用户能够在“我的记忆”页面修改或删除单条记忆。

---

## 10. 技术栈总览

### 10.1 最终选型

| 层次 | 最终选择 | 用途 |
|---|---|---|
| 前端语言 | TypeScript | 类型安全和复杂交互维护 |
| 前端框架 | React | SPA、生态和组件复用 |
| 构建工具 | Vite | 开发启动和生产构建 |
| UI | Ant Design | 表格、表单、上传、步骤条等后台型组件 |
| 数据请求 | TanStack Query | 服务端状态、缓存和失效 |
| 本地状态 | Zustand | 对话草稿、页面 UI 状态 |
| 路由 | React Router | 页面路由 |
| 流式传输 | SSE | Agent 单向事件流 |
| 后端语言 | Python 3.12 | AI 生态兼容性和稳定性 |
| Web 框架 | FastAPI | 异步 API、类型模型、OpenAPI |
| 数据校验 | Pydantic v2 | 请求、工具和模型输出校验 |
| ORM | SQLAlchemy 2.x | 数据访问和事务 |
| 数据迁移 | Alembic | 数据库版本管理 |
| Agent 编排 | LangGraph | 状态图、Checkpoint、Interrupt |
| 业务数据库 | PostgreSQL | 结构化数据事实来源 |
| 向量数据库 | Qdrant | Dense/Sparse 检索和过滤 |
| 缓存与 Broker | Redis | Celery、缓存和事件协调 |
| 异步任务 | Celery | 文档解析、索引和评测 |
| 对象存储 | MinIO | 原始文件、报告和导出文件 |
| 文档解析 | PyMuPDF、python-docx | PDF/DOCX 解析 |
| 模型接口 | OpenAI-compatible Adapter | 可替换生成模型 |
| Embedding | BGE-M3 或可配置 API | 中英文语义和 Sparse 表示 |
| Reranker | BGE Reranker 或 API | 二阶段相关性排序 |
| MCP | 官方 Python SDK/兼容客户端 | 外部工具接入 |
| 测试 | pytest、Vitest、Playwright | 单元、集成和 E2E |
| Python 规范 | Ruff、Pyright | 格式、Lint、类型检查 |
| 前端规范 | ESLint、Prettier | 代码规范 |
| 容器 | Docker Compose | 本地完整运行 |
| 可观测 | 结构化日志 + OpenTelemetry | Trace、Metric、Log |
| LLM 观测 | 自建 Run 页面，Langfuse 可选 | 调用链与成本分析 |

### 10.2 版本策略

- Python 固定为 3.12 系列，优先兼容 AI 生态，不盲目使用刚发布的大版本。
- Node.js 使用当前 LTS 版本。
- Python 使用 uv 管理依赖和锁文件。
- 前端使用 pnpm 和 pnpm-lock.yaml。
- Docker 镜像使用明确版本或 digest，不在正式环境使用 latest。
- 所有模型记录 model_id、revision 和 embedding_dimensions。
- 每次升级 LangGraph、Pydantic、Qdrant Client 前运行完整评测。

---

## 11. 关键技术选型依据

### 11.1 为什么选择 Python，而不是 Java

选择 Python 的原因：

- LangGraph、Embedding、Reranker、文档解析和评测生态更完整。
- 模型 SDK 和 MCP Python SDK 接入成本更低。
- Agent 状态模型、Pydantic Schema 与 FastAPI 能共享类型。
- 一个开发者可以在同一语言内完成 API、RAG、Agent、Worker 和评测。
- 项目重点是 AI 应用工程，不需要为了展示语言而引入 Java 服务。

代价：

- CPU 密集任务需要 Worker 或独立进程，不能阻塞 API。
- Python 类型约束弱于编译型语言，因此强制使用 Pyright、Pydantic 和测试。
- 生产并发需要正确使用异步 I/O 和多进程。

### 11.2 为什么选择 FastAPI，而不是 Django、Flask

选择 FastAPI：

- 原生支持异步接口，适合模型调用、SSE 和 MCP I/O。
- Pydantic 可以同时用于 API、Tool Schema 和结构化模型输出。
- 自动生成 OpenAPI，便于前端生成客户端类型。
- 相比 Django 更轻，适合 API 优先项目。
- 相比 Flask 有更完整的类型、依赖注入和文档支持。

不选择 Django：

- 本项目没有强依赖 Django Admin 和模板系统。
- Agent、RAG 服务以异步 API 为主。
- Django 可以完成，但会引入当前不需要的全栈约定。

不选择 Flask：

- 自行组合校验、OpenAPI、依赖注入和异步能力的成本更高。

### 11.3 为什么选择 React，而不是 Vue

两者都能完成项目。最终选择 React 的原因：

- Agent Chat、运行轨迹和可视化组件可以按 Feature 组合。
- TanStack Query、React Router、SSE 相关生态成熟。
- React + TypeScript 在 AI 应用前端示例和组件生态中覆盖广。
- 对未来接入富文本、代码展示、图表和流式 UI 更方便。
- 作为简历项目，能够展示主流前端工程能力。

没有选择 Next.js：

- 第一版是登录后的交互式工作台，不依赖 SEO 和服务端渲染。
- 独立 FastAPI 已承担后端，Next.js Server 反而形成双后端。
- Vite SPA 更简单，部署和调试边界清晰。

如果开发者更熟悉 Vue，Vue 3 + TypeScript 也完全可行；这是团队熟悉度选择，不是能力差异。

### 11.4 为什么选择 LangGraph，而不是纯 LangChain Agent 或自研循环

选择 LangGraph：

- 模拟面试、审批和学习计划是明确的有状态长流程。
- 需要 Checkpoint、暂停、恢复、状态历史和故障恢复。
- 可以用条件边表达追问、结束、审批和失败分支。
- 节点仍然是普通 Python 函数，便于测试。

不直接使用自由循环 Agent：

- 难以控制最大步骤数和副作用。
- 难以恢复到中间状态。
- 同一输入的执行路径不稳定。
- 不利于精确评测每个节点。

不完全自研状态机：

- Checkpoint、Interrupt、恢复和状态历史容易出现边界错误。
- LangGraph 已提供所需基础能力。

仍需自研 Harness。LangGraph 只是编排引擎，不负责项目级权限、成本、Skill、MCP、Trace 和评测。

### 11.5 向量数据库比较

#### 11.5.1 对比结论

| 方案 | 优点 | 缺点 | 本项目结论 |
|---|---|---|---|
| Qdrant | 单容器、本地 Dashboard、Payload Filter、Dense/Sparse、Hybrid Query、Python 友好 | 增加独立数据服务，需要维护与 PostgreSQL 一致性 | 第一版选择 |
| Milvus | 大规模能力强、部署形态丰富、Dense/Sparse/BM25/Hybrid 完整 | Standalone 资源较重；Windows 本地开发不如 Qdrant 直接；运维组件更多 | 数据规模扩大后候选 |
| pgvector | 与 PostgreSQL 共库，部署最简单，事务一致性好 | 向量与业务负载耦合；高级 Hybrid/Rerank 需要更多应用层实现 | 极简 MVP 候选 |
| Elasticsearch/OpenSearch | BM25、中文分词、过滤和日志检索强 | 资源较重；向量检索配置复杂；本项目还需 PostgreSQL | 不选择 |
| Chroma | 原型快速、API 简单 | 生产治理、复杂过滤和规模演进不如专用服务 | 仅实验 |
| FAISS | 本地库、检索性能高 | 不是数据库，缺少持久化、并发、权限、过滤和服务治理 | 不选择 |
| Weaviate | Hybrid 和 Schema 能力完善 | 本地服务和概念相对更多，项目无明显收益 | 备选 |

#### 11.5.2 为什么最终选择 Qdrant

第一，符合本项目数据规模。个人知识库预计为几千到几十万个 Chunk，不需要一开始部署面向超大规模的分布式系统。

第二，本地启动简单。Qdrant 官方支持 Docker 单容器运行，并提供 REST、gRPC 和本地 Dashboard，适合课程演示和 Windows Docker Desktop。

第三，能够完成项目真正需要的检索：

- 多个 Named Vectors。
- Dense 和 Sparse 检索。
- RRF 等融合方式。
- 多阶段 Query 和 Rerank。
- Payload 元数据过滤。
- 按 workspace_id、document_type、version 做权限过滤。

第四，Python 客户端和 FastAPI 集成直接，开发与调试成本低。

第五，架构仍然保留 VectorStorePort，未来可以迁移，不在业务层直接依赖 Qdrant 返回类型。

#### 11.5.3 为什么第一版不选择 Milvus

Milvus 本身能力很强，不选择它不是因为功能不足，而是当前约束不匹配：

- 官方文档中 Milvus Standalone 最低建议 8GB RAM，推荐 16GB。
- Standalone 传统部署涉及 etcd、对象存储等组件，整体资源和运维复杂度高于 Qdrant 单容器。
- Milvus Lite 使用简单，并与 Standalone/Distributed 共享 API，但官方当前支持列表不包含 Windows。
- 本项目首先要求在 Windows 本地稳定、完整演示。
- 预期向量规模没有达到必须使用 Milvus 分布式能力的程度。

Milvus 的优势：

- 从 Lite、Standalone 到 Distributed 的扩展路径。
- 原生 BM25、Sparse、Dense 和 Hybrid Search。
- 大规模向量和多租户能力。
- 更适合百万到十亿级向量、独立搜索团队和集群化部署。

切换到 Milvus 的触发条件：

- 单 Collection 达到千万级向量并经过真实压测。
- 需要独立扩缩搜索节点。
- 需要 Milvus 特定索引或分布式能力。
- 部署环境从个人 Windows 转为 Linux/Kubernetes。
- 团队可以承担独立集群运维。

因此文档的结论是“当前选择 Qdrant，保留 Milvus 迁移接口”，而不是为了简历关键词直接使用 Milvus。

#### 11.5.4 为什么不只使用 pgvector

pgvector 是最简洁的方案，适合非常小的 MVP，但本项目希望真实展示：

- Dense + Sparse 混合检索。
- 多阶段查询。
- 独立向量 Payload Filter。
- 向量索引独立扩展。

如果项目目标变成“一周内做出最小版本”，应重新考虑 pgvector；如果目标是完整简历项目，Qdrant 的检索实验空间更合适。

### 11.6 为什么选择 PostgreSQL

- 用户、工作空间、消息、面试、计划、审批等数据关系明确。
- 需要事务、唯一约束、外键和审计。
- JSONB 可保存部分可变的 Agent 配置。
- LangGraph 可使用 PostgreSQL Checkpointer。
- 比 MongoDB 更适合能力项、证据映射和任务关系。

PostgreSQL 是事实来源，Qdrant 只是可重建的检索索引。

### 11.7 为什么同时使用 Redis 和 Celery

文档解析、Embedding 和批量评测可能持续数十秒到数分钟，不能占用 API 请求。

Celery 负责：

- 文档解析和索引。
- 批量 Embedding。
- RAG 离线评测。
- 报告导出。
- 定时计划复盘。

Redis 负责：

- Celery Broker。
- 短期缓存。
- 限流计数。
- 跨进程事件协调。

不使用 Kafka，因为当前没有大规模事件流、复杂消费者组和高吞吐日志管道。

### 11.8 为什么选择 MinIO

- 原始 PDF、DOCX 和导出报告不适合放入数据库。
- MinIO 提供与 S3 兼容的对象存储接口。
- 本地 Docker 可运行，未来可以平滑切换云端 S3。
- 支持预签名 URL 和 Bucket 权限。

如果第一版开发资源非常紧张，可以先使用受控本地目录，但正式演示版本使用 MinIO。

### 11.9 为什么使用 SSE，而不是 WebSocket

主要场景是后端向前端持续推送：

- Token 增量。
- Agent 步骤。
- 检索进度。
- 审批等待。
- 任务完成或失败。

SSE 的单向通道已经足够，并具备：

- 基于 HTTP，代理配置简单。
- 浏览器自动重连。
- 易于携带 Last-Event-ID。
- 比 WebSocket 更容易调试。

用户输入仍通过普通 POST 请求发送。只有将来加入实时语音或双向协作时再使用 WebSocket。

### 11.10 为什么不把 Langfuse 作为必需依赖

项目需要展示 Agent 轨迹，因此首先在业务数据库中保存 Run 和 Step，保证用户页面可用。

Langfuse 可作为开发和分析增强：

- LLM Trace。
- Prompt 管理。
- Token 和成本分析。

但它不应成为核心业务运行的单点依赖。没有 Langfuse 时，项目仍应完整运行。

---

## 12. 前端架构

### 12.1 页面结构

| 路由 | 页面 | 主要功能 |
|---|---|---|
| /login | 登录 | 本地账户登录 |
| /workspaces | 工作空间 | 创建、切换、归档 |
| /w/:id/chat | 智能对话 | 流式回答、引用、审批卡片 |
| /w/:id/knowledge | 知识库 | 上传、解析状态、文档详情 |
| /w/:id/jobs | JD 管理 | 录入、比较和能力抽取 |
| /w/:id/gap | 能力差距 | 要求、证据、覆盖和建议 |
| /w/:id/interviews | 模拟面试 | 会话列表和报告 |
| /w/:id/interviews/:session | 面试会话 | 单题对话、追问、计时 |
| /w/:id/plans | 学习计划 | 日历、任务和动态调整 |
| /w/:id/memory | 我的记忆 | 查看、确认、修改、删除 |
| /w/:id/runs/:runId | Agent 轨迹 | 节点、工具、检索、成本 |
| /w/:id/evals | 评测中心 | 数据集、实验和对比 |
| /settings | 设置 | 模型、MCP、存储和安全 |

### 12.2 Feature 目录

    frontend/
      src/
        app/
          router/
          providers/
          layouts/
        features/
          auth/
          workspace/
          chat/
          knowledge/
          job-analysis/
          interview/
          study-plan/
          memory/
          agent-run/
          evaluation/
          settings/
        components/
          ui/
          citation/
          streaming/
          approval/
        api/
          client/
          generated/
          sse/
        stores/
        hooks/
        types/
        utils/
        tests/

按业务 Feature 组织，而不是把所有组件、Hooks、API 分别堆在巨大目录中。

### 12.3 前端状态划分

TanStack Query 管理服务端状态：

- 文档列表。
- 面试记录。
- 学习计划。
- Agent Run。
- 用户记忆。

Zustand 只管理本地 UI 状态：

- 当前对话草稿。
- 展开的引用面板。
- 未提交的筛选条件。
- 当前 SSE 连接状态。

不将服务端实体重复复制到全局 Store。

### 12.4 Chat UI

消息类型：

- user
- assistant
- system_notice
- tool_status
- approval_request
- error

Assistant 消息支持：

- Markdown。
- 流式增量。
- 引用角标。
- 结构化表格。
- Skill 标签。
- 复制、重试和反馈。

审批卡片必须展示：

- 即将调用的工具。
- 操作目标。
- 关键参数。
- 风险等级。
- 批准、修改、拒绝按钮。

### 12.5 SSE 事件格式

统一事件：

    event: run.started
    data: {"run_id":"..."}

    event: step.started
    data: {"step_id":"...","name":"retrieve"}

    event: message.delta
    data: {"text":"..."}

    event: approval.required
    data: {"approval_id":"...","tool":"calendar.create_event"}

    event: run.completed
    data: {"run_id":"...","result":{}}

    event: run.failed
    data: {"code":"MODEL_TIMEOUT","message":"..."}

前端使用 event_id 去重，并使用 Last-Event-ID 恢复断线后的事件。

### 12.6 可访问性和交互要求

- 所有操作支持键盘访问。
- 颜色不能是状态的唯一表达方式。
- 上传和索引进度有文本提示。
- 流式输出期间允许停止生成。
- 长运行任务关闭页面后仍可继续。
- 错误提示包含可执行的恢复建议。

---

## 13. 后端架构与目录

### 13.1 推荐目录

    backend/
      pyproject.toml
      alembic.ini
      app/
        main.py
        api/
          dependencies.py
          errors.py
          v1/
            auth.py
            workspaces.py
            documents.py
            conversations.py
            agent_runs.py
            jobs.py
            interviews.py
            plans.py
            memories.py
            approvals.py
            evaluations.py
        core/
          config.py
          security.py
          logging.py
          telemetry.py
          exceptions.py
        db/
          session.py
          models/
          repositories/
          migrations/
        domains/
          workspace/
          document/
          conversation/
          job/
          interview/
          planning/
          memory/
          evaluation/
        agent/
          state.py
          graphs/
          nodes/
          harness/
            run_manager.py
            context_builder.py
            model_gateway.py
            tool_registry.py
            policy_engine.py
            checkpoint.py
            trace_manager.py
        rag/
          parsers/
          chunkers/
          embeddings/
          sparse/
          rerankers/
          retrievers/
          citations/
          vector_store/
        skills/
        mcp/
          client_manager.py
          adapters/
          security.py
        services/
          storage/
          encryption/
          events/
        workers/
          celery_app.py
          ingestion_tasks.py
          evaluation_tasks.py
        schemas/
        tests/
          unit/
          integration/
          contract/
      evals/
        datasets/
        experiments/
        reports/

    frontend/
      ...

    infra/
      docker-compose.yml
      docker-compose.dev.yml
      nginx/
      postgres/
      qdrant/
      minio/

    docs/
      architecture/
      api/
      decisions/

    scripts/
      bootstrap.ps1
      bootstrap.sh
      seed_demo_data.py

    .env.example
    README.md
    开发文档.md

### 13.2 分层规则

- API 层只处理协议、认证、校验和响应转换。
- Domain Service 实现业务规则，不直接依赖 FastAPI。
- Repository 封装 PostgreSQL 访问。
- Agent Node 调用 Domain Service 或明确的 Port。
- RAG Service 不直接修改业务实体。
- Qdrant 返回类型不进入 Domain 层。
- MCP Adapter 将外部 Schema 转换成内部模型。
- Worker 复用 Service，不复制业务逻辑。

### 13.3 Port 与 Adapter

需要定义以下抽象：

- ModelProvider
- EmbeddingProvider
- SparseProvider
- RerankProvider
- VectorStorePort
- ObjectStoragePort
- CalendarPort
- CodeRepositoryPort
- EventPublisher

目的不是追求抽象数量，而是隔离真正可能替换或属于外部系统的组件。

---

## 14. 数据模型

### 14.1 核心实体

#### users

- id
- email
- password_hash
- display_name
- status
- created_at
- updated_at

#### workspaces

- id
- user_id
- name
- target_role
- target_date
- daily_minutes
- timezone
- status
- created_at

#### documents

- id
- workspace_id
- type
- display_name
- active_version_id
- status
- created_at

#### document_versions

- id
- document_id
- version
- object_key
- mime_type
- file_size
- sha256
- parser_version
- indexing_status
- error_code
- created_at

#### document_chunks

- id
- document_version_id
- ordinal
- title_path
- page_number
- start_offset
- end_offset
- content
- token_count
- content_hash
- embedding_version
- is_active

#### conversations

- id
- workspace_id
- title
- summary
- created_at
- updated_at

#### messages

- id
- conversation_id
- role
- content
- structured_content
- model_id
- token_usage
- created_at

#### agent_runs

- id
- workspace_id
- conversation_id
- thread_id
- intent
- skill_name
- skill_version
- status
- input
- output
- total_tokens
- estimated_cost
- started_at
- completed_at

#### agent_run_steps

- id
- run_id
- parent_step_id
- node_name
- status
- input_summary
- output_summary
- model_id
- prompt_version
- latency_ms
- token_usage
- error_code
- started_at
- completed_at

#### tool_calls

- id
- run_id
- step_id
- source
- server_name
- tool_name
- risk_level
- arguments_encrypted_or_redacted
- result_summary
- idempotency_key
- status
- latency_ms

#### approvals

- id
- run_id
- tool_call_id
- status
- request_payload
- user_response
- expires_at
- decided_at

#### job_descriptions

- id
- workspace_id
- document_id
- company
- title
- source_url
- raw_text
- parsed_version

#### competencies

- id
- workspace_id
- canonical_name
- category
- description

#### job_requirements

- id
- job_description_id
- competency_id
- importance
- requirement_type
- raw_evidence

#### evidence_links

- id
- workspace_id
- competency_id
- source_type
- source_id
- chunk_id
- support_level
- confidence
- explanation

#### interview_sessions

- id
- workspace_id
- job_description_id
- type
- status
- difficulty
- started_at
- ended_at
- total_score

#### interview_turns

- id
- session_id
- turn_number
- question
- answer
- follow_up_reason
- rubric
- score
- feedback
- created_at

#### study_plans

- id
- workspace_id
- goal
- start_date
- end_date
- version
- status
- created_at

#### study_tasks

- id
- plan_id
- competency_id
- title
- description
- scheduled_at
- duration_minutes
- priority
- status
- external_event_id

#### memories

- id
- workspace_id
- type
- key
- value
- source_type
- source_id
- confidence
- user_confirmed
- status
- expires_at
- created_at
- updated_at

#### evaluation_cases

- id
- dataset
- input
- expected
- tags
- version

#### evaluation_results

- id
- case_id
- experiment_id
- config_snapshot
- metrics
- trace_id
- created_at

### 14.2 数据一致性

PostgreSQL 为事实来源，Qdrant 为派生索引。

索引写入采用任务状态和幂等策略：

1. PostgreSQL 创建 Chunk。
2. Worker 生成向量。
3. 按 chunk_id Upsert Qdrant。
4. 成功后更新 embedding_version 和状态。
5. 文档删除时先标记 inactive，再异步删除 Qdrant 点。
6. 定期运行 reconciliation job 检查缺失或孤立索引。

不要使用跨 PostgreSQL 和 Qdrant 的伪分布式事务。

---

## 15. API 设计

统一前缀：/api/v1  
认证：HttpOnly Session Cookie 或短期 Access Token + Refresh Token。  
响应包含 request_id，错误使用稳定 error_code。

### 15.1 文档

| 方法 | 路径 | 作用 |
|---|---|---|
| POST | /workspaces/{id}/documents | 上传文档 |
| GET | /workspaces/{id}/documents | 文档列表 |
| GET | /documents/{id} | 文档详情 |
| GET | /documents/{id}/chunks | 查看切分 |
| POST | /documents/{id}/reindex | 重新索引 |
| DELETE | /documents/{id} | 删除单个文档 |

### 15.2 对话与 Agent

| 方法 | 路径 | 作用 |
|---|---|---|
| POST | /conversations | 创建对话 |
| GET | /conversations/{id}/messages | 获取消息 |
| POST | /agent/runs | 创建 Agent Run |
| GET | /agent/runs/{id} | 运行详情 |
| GET | /agent/runs/{id}/events | SSE 事件 |
| POST | /agent/runs/{id}/cancel | 取消运行 |
| POST | /agent/runs/{id}/retry | 从安全节点重试 |

### 15.3 审批

| 方法 | 路径 | 作用 |
|---|---|---|
| GET | /approvals/{id} | 获取待审批动作 |
| POST | /approvals/{id}/approve | 批准 |
| POST | /approvals/{id}/reject | 拒绝 |
| POST | /approvals/{id}/edit | 修改参数后批准 |

### 15.4 JD 与能力

| 方法 | 路径 | 作用 |
|---|---|---|
| POST | /workspaces/{id}/jobs | 创建 JD |
| POST | /jobs/{id}/analyze | 分析 JD |
| GET | /jobs/{id}/requirements | 岗位要求 |
| POST | /jobs/compare | 比较多个 JD |
| GET | /workspaces/{id}/competency-gap | 能力差距 |

### 15.5 模拟面试

| 方法 | 路径 | 作用 |
|---|---|---|
| POST | /interviews | 创建会话 |
| POST | /interviews/{id}/start | 开始 |
| POST | /interviews/{id}/answers | 提交回答 |
| POST | /interviews/{id}/finish | 提前结束 |
| GET | /interviews/{id}/report | 报告 |

### 15.6 学习计划与记忆

| 方法 | 路径 | 作用 |
|---|---|---|
| POST | /plans/generate | 生成计划 |
| GET | /plans/{id} | 获取计划 |
| PATCH | /tasks/{id} | 更新任务状态 |
| POST | /plans/{id}/reschedule | 重排 |
| GET | /workspaces/{id}/memories | 记忆列表 |
| PATCH | /memories/{id} | 修改/确认 |
| DELETE | /memories/{id} | 删除单条记忆 |

### 15.7 API 契约

- OpenAPI 是前后端契约来源。
- 前端客户端从 OpenAPI 生成 TypeScript 类型。
- Agent 输出先转换为业务 DTO，不直接返回 LangGraph 内部 State。
- 分页使用 cursor，日志和步骤列表不使用不稳定的 offset。
- 所有时间使用 UTC 存储，前端按工作空间时区显示。

---

## 16. 安全设计

### 16.1 用户与数据隔离

- 每个业务查询必须包含 user_id 或 workspace_id 范围。
- RAG 查询强制添加 workspace_id Payload Filter。
- 文档下载使用短期预签名 URL。
- 不允许客户端指定任意对象存储 Key。
- 测试中加入跨工作空间越权用例。

### 16.2 文件安全

- 文件名不作为物理路径。
- 检查 MIME、扩展名、大小和魔数。
- 限制 PDF 页数和解压后大小。
- 文档解析在 Worker 进程执行。
- 不执行上传文件中的宏、脚本或嵌入附件。
- 对解析超时和内存使用设置限制。

### 16.3 Prompt Injection

遵循以下规则：

- 上传文档、网页、MCP 返回内容全部视为不可信数据。
- 外部内容不能覆盖系统规则、Skill 权限和 Tool Policy。
- 模型只提出 Tool Request，最终执行由 Harness 决定。
- 工具参数不允许直接由文档中的隐藏指令生成并执行。
- 敏感工具要求用户确认。
- RAG 上下文标注来源边界。

### 16.4 密钥

- API Key 不存入 Git。
- 使用环境变量或加密后的数据库字段。
- 加密主密钥由部署环境提供。
- 日志和 Trace 对密钥、Token、Cookie 自动脱敏。
- 前端永远不直接持有第三方模型的长期密钥。

### 16.5 Agent 限制

- 每次运行最大步骤数。
- 最大模型调用次数。
- 最大 Tool Call 次数。
- 最大上下文 Token。
- 单工具超时。
- 总运行超时。
- 每用户并发运行数。
- 高成本模型预算。

---

## 17. 非功能要求

### 17.1 性能目标

在 10 万 Chunk 的本地测试集下：

- 普通业务 API P95 小于 300ms，不含模型调用。
- 混合检索 P95 小于 800ms，不含远程 Embedding。
- 文档上传接口 2 秒内返回任务 ID。
- SSE 首个状态事件 1 秒内返回。
- 用户可以在 2 秒内取消尚未进入不可取消副作用的运行。

生成模型首字延迟依赖提供商，必须单独记录，不能与本地 API 延迟混为一个指标。

### 17.2 可用性

- 单个 MCP Server 故障不影响本地知识问答。
- Reranker 故障时降级到 Hybrid 结果。
- Worker 重启后未确认任务可以重新领取。
- Agent 等待审批时 API 重启后能够恢复。
- 重复提交回答和审批不会产生重复副作用。

### 17.3 可维护性

- 后端关键业务类型覆盖 Pyright。
- 所有公共 API 有 Schema。
- 核心 Domain Service 单元测试覆盖率目标 80%。
- 所有数据库变更通过 Alembic。
- 架构决策写入 docs/decisions。
- 关键实验保留配置快照和结果。

### 17.4 隐私

- 用户能导出个人数据。
- 用户能删除单个文档、记忆和会话。
- 删除操作同时安排索引和对象文件清理。
- 默认不使用用户数据训练模型。
- 调用外部模型前在设置页说明哪些文本会被发送。

---

## 18. 本地开发与部署

### 18.1 环境要求

- Windows 11 + WSL2 或 Linux/macOS。
- Docker Desktop / Docker Engine。
- Python 3.12。
- Node.js 当前 LTS。
- uv。
- pnpm。
- Git。

开发者可使用本机运行前后端、Docker 运行基础设施；验收时必须支持 Docker Compose 完整启动。

### 18.2 环境变量

.env.example 至少包含：

    APP_ENV=development
    APP_SECRET_KEY=change-me
    DATABASE_URL=postgresql+asyncpg://...
    REDIS_URL=redis://redis:6379/0
    QDRANT_URL=http://qdrant:6333
    MINIO_ENDPOINT=minio:9000
    MINIO_ACCESS_KEY=...
    MINIO_SECRET_KEY=...
    LLM_BASE_URL=...
    LLM_API_KEY=...
    LLM_MODEL=...
    EMBEDDING_PROVIDER=local
    EMBEDDING_MODEL=BAAI/bge-m3
    RERANK_PROVIDER=local
    RERANK_MODEL=BAAI/bge-reranker-v2-m3
    ENCRYPTION_KEY=...

真实 .env 不提交版本库。

### 18.3 启动模式

#### 开发模式

- 基础设施使用 Docker Compose。
- FastAPI 本机启动，支持热更新。
- Celery Worker 本机启动。
- React Vite Dev Server 本机启动。

#### 演示模式

一个命令启动：

    docker compose up --build

启动后：

- Web：http://localhost:3000
- API 文档：http://localhost:8000/docs
- Qdrant Dashboard：http://localhost:6333/dashboard
- MinIO Console：http://localhost:9001

Windows 下 Qdrant 使用 Docker Named Volume，避免宿主机目录挂载兼容问题。

### 18.4 数据初始化

提供 seed_demo_data.py：

- 创建演示用户和工作空间。
- 导入脱敏简历。
- 导入 3 个示例 JD。
- 导入项目 README 和学习笔记。
- 创建一场已完成的模拟面试。
- 生成基础评测数据。

演示数据必须脱敏且可公开。

### 18.5 数据备份

本地演示版本至少支持：

- PostgreSQL 逻辑备份。
- MinIO Bucket 导出。
- Qdrant Snapshot。
- 配置文件备份。

由于 Qdrant 可由 PostgreSQL Chunk 重建，恢复顺序为 PostgreSQL、MinIO、Qdrant；若 Qdrant Snapshot 不可用则运行重建任务。

---

## 19. 测试策略

### 19.1 单元测试

覆盖：

- JD 规范化。
- 能力覆盖计算。
- Chunk 切分。
- RRF 融合。
- 引用解析和校验。
- Memory 更新。
- Policy Engine。
- Tool Schema。
- 幂等逻辑。

模型调用全部使用 Fake Provider，不在普通单元测试中访问外部 API。

### 19.2 集成测试

使用测试容器启动：

- PostgreSQL。
- Redis。
- Qdrant。
- MinIO。

覆盖：

- 文档上传到索引完成。
- RAG 检索和权限过滤。
- Checkpoint 暂停与恢复。
- Celery 重试。
- 单文档删除后的索引清理。
- API 数据库事务。

### 19.3 Contract Test

- OpenAPI 向后兼容检查。
- MCP Tool Schema 兼容。
- Model Provider 结构化输出。
- Skill Manifest Schema。
- SSE Event Schema。

### 19.4 前端测试

Vitest：

- Citation 组件。
- 审批卡片。
- 流式消息 Reducer。
- 表单和状态转换。

Playwright：

- 登录。
- 上传简历。
- 等待索引完成。
- 发起带引用问答。
- 创建模拟面试。
- 完成一次问答。
- 批准日历写入的 Mock 流程。

### 19.5 Agent 评测

评测分层：

| 层 | 指标 |
|---|---|
| Router | 意图与 Skill 路由准确率 |
| Retrieval | Recall、MRR、Context Precision |
| Generation | Faithfulness、Citation Precision |
| Tool | 工具选择、参数和执行成功率 |
| Workflow | 任务完成率、步骤数、恢复成功率 |
| Business | 差距分析准确性、面试评分一致性 |
| Runtime | Token、费用、P95 延迟 |

LLM-as-a-Judge 不能是唯一评测。对关键数据保留人工标注和确定性检查。

### 19.6 安全测试

- 跨工作空间检索。
- 恶意文件名和路径穿越。
- 文档中的 Prompt Injection。
- MCP 返回恶意指令。
- 未审批的外部写入。
- 重放审批请求。
- 重复日历调用。
- 超大文件和解析炸弹。

---

## 20. 开发里程碑

### 阶段 0：工程骨架

目标：

- 建立前后端仓库结构。
- Docker Compose 启动 PostgreSQL、Redis、Qdrant、MinIO。
- FastAPI 健康检查。
- React 基础布局。
- CI、Lint、类型检查和测试骨架。

验收：

- 新开发者按照 README 在 30 分钟内启动项目。

### 阶段 1：知识库与基础 RAG

目标：

- 文档上传、解析、Chunk、Embedding、Qdrant 索引。
- 知识库管理页面。
- Dense 检索和引用问答。
- Agent Run 与 SSE 基础能力。

验收：

- 上传简历后可提出事实问题并点击查看引用。

### 阶段 2：Hybrid RAG 与评测

目标：

- Sparse、RRF、Reranker。
- 文档版本、去重和重建。
- RAG 评测数据集和对比报告。

验收：

- 提供 Dense 与 Hybrid 的量化对比，而不是主观截图。

### 阶段 3：JD 分析

目标：

- JD 结构化抽取。
- 能力标准化。
- 证据匹配。
- 差距分析页面。
- 多 JD 对比。

验收：

- 每个覆盖结论可以追溯到 JD 和个人资料。

### 阶段 4：模拟面试与记忆

目标：

- 面试 LangGraph。
- 动态追问。
- 最终 Rubric 评分。
- 能力画像和记忆管理。

验收：

- 完成一场 10 题左右的面试，中断后可以恢复。

### 阶段 5：学习计划

目标：

- 根据能力差距生成计划。
- 任务完成和动态重排。
- 日、周视图。

验收：

- 面试结果变化能够影响后续任务优先级。

### 阶段 6：Skill、MCP 与审批

目标：

- Skill Registry 和版本记录。
- GitHub 只读 MCP。
- Calendar MCP Mock 或真实沙箱。
- Tool Policy、Interrupt 和审批 UI。

验收：

- 创建日历前暂停；重启服务后仍能批准并恢复。

### 阶段 7：工程化与作品包装

目标：

- 完整测试。
- 性能和成本报告。
- 安全检查。
- 演示数据和演示视频。
- README、架构图、接口文档。

验收：

- 项目可本地完整运行，无核心功能卡死或崩溃。
- README 包含一键启动、演示账号、架构、指标和截图。

---

## 21. 验收标准

### 21.1 功能验收

- 能上传并解析 PDF/DOCX/Markdown。
- 能对文档进行版本化和单文件删除。
- 回答至少展示文档名、页码和原文片段。
- JD 分析包含岗位要求、证据、覆盖程度和建议。
- 模拟面试可以动态追问，而不是固定题库播放。
- 会话和面试可以跨页面恢复。
- 长期记忆可查看、确认、修改和删除。
- 学习计划可执行并根据表现调整。
- Agent Run 页面能展示主要执行轨迹。
- 外部写操作未经批准不能执行。

### 21.2 工程验收

- Docker Compose 可以完整启动。
- API、Worker、PostgreSQL、Redis、Qdrant、MinIO 有健康检查。
- 数据库变更全部通过 Alembic。
- 核心模块有单元和集成测试。
- CI 自动执行 Lint、类型检查和测试。
- API Key 和个人文件不进入 Git。
- README 可以让新用户独立运行。

### 21.3 质量验收

建议首个可发布版本达到：

- Skill 路由准确率不低于 90%。
- 结构化输出 Schema 通过率不低于 98%。
- Citation Precision 不低于 90%。
- 无证据问题正确拒答率不低于 85%。
- Tool 参数校验通过率不低于 98%。
- 外部写操作审批覆盖率为 100%。
- Agent 中断恢复测试通过率为 100%。

具体指标需要在固定测试集上报告，不能只写在 README 中而不提供实验数据。

---

## 22. 可观测与评测页面

### 22.1 Run 详情

展示：

- 用户输入。
- 识别意图和路由 Skill。
- LangGraph 节点时间线。
- 每个节点状态和耗时。
- 检索查询和 Top-K。
- Tool Call 和审批。
- 模型、Token 和费用。
- 最终输出。
- 错误和重试。

### 22.2 RAG 实验

支持建立实验：

- 选择评测集。
- 选择 Embedding。
- 选择 Chunk 参数。
- 选择 Dense/Sparse。
- 选择融合策略。
- 选择 Reranker。
- 运行并生成指标。

对比页面展示：

- Recall@K。
- MRR。
- Faithfulness。
- Citation Precision。
- P95 延迟。
- 单次平均成本。

### 22.3 Skill 实验

- 选择 Skill 版本。
- 选择模型。
- 运行固定用例。
- 对比任务完成率、Tool Accuracy 和 Schema 通过率。
- 保存失败案例用于回归测试。

---

## 23. 风险与应对

| 风险 | 表现 | 应对 |
|---|---|---|
| 模型输出不稳定 | JSON 缺字段、评分漂移 | Pydantic 校验、重试、固定评测集 |
| 引用幻觉 | 生成不存在页码 | 后端只接受上下文内 citation_id |
| 中文 Sparse 效果差 | 技术词召回不稳定 | 评测 BGE-M3 Sparse、BM25 和分词方案 |
| 本地模型过慢 | 首字延迟高 | Provider 可配置，默认允许远程 API |
| 文档解析失败 | 扫描件、复杂表格 | 显示失败原因，OCR 作为 P1 |
| Qdrant 与 PostgreSQL 不一致 | 孤立或缺失向量 | 幂等 Upsert、状态机、Reconciliation |
| Agent 重复副作用 | 创建重复日历 | Checkpoint 规则、幂等键、审批 |
| Prompt Injection | 文档诱导调用工具 | 内容隔离、工具白名单、Policy Engine |
| 项目范围失控 | 技术堆叠、进度拖延 | 按 P0/P1/P2 和里程碑验收 |
| 评分缺乏可信度 | LLM 自评偏高 | 独立评分调用、Rubric、人工样本 |
| 隐私泄露 | 简历发送到外部模型 | 明示数据流、脱敏、可选本地模型 |

---

## 24. 简历项目表达建议

项目完成后，简历不应只写：

> 使用 LangChain、RAG 和大模型开发智能面试系统。

应写成包含业务、架构和量化结果的描述，例如：

- 设计并实现基于 FastAPI、LangGraph 和 React 的求职准备 Agent，通过状态图完成 JD 分析、项目证据匹配、自适应面试和学习计划闭环。
- 构建 Dense + Sparse + RRF + Reranker 混合检索链路，在固定评测集上将 Recall@5 从实际基线提升至实测结果，并实现可验证的页码级引用。
- 设计 Skill Registry、Tool Policy 和 Human-in-the-loop Harness，实现 Tool Schema 校验、运行持久化、审批恢复和外部写入幂等控制。
- 基于 MCP 接入 GitHub 与日历，使 Agent 能从真实代码提取项目证据，并在用户确认后同步学习计划。
- 建立 Agent/RAG 评测与 Trace 页面，量化任务完成率、工具准确率、引用正确率、P95 延迟和 Token 成本。

注意：上面“提升至实测结果”必须在项目完成后替换成真实数字，不能预先编造。

---

## 25. 架构决策记录

建议在 docs/decisions 中维护 ADR：

| ADR | 主题 |
|---|---|
| ADR-001 | 使用模块化单体而非微服务 |
| ADR-002 | 使用 Qdrant 而非 Milvus |
| ADR-003 | PostgreSQL 作为事实来源 |
| ADR-004 | 使用 LangGraph 作为状态编排 |
| ADR-005 | 使用 SSE 传输 Agent 事件 |
| ADR-006 | 外部写操作强制审批 |
| ADR-007 | Skill 使用文件规范和版本管理 |
| ADR-008 | MCP 只用于独立外部边界 |

每个 ADR 记录背景、备选方案、决策、后果和重新评估条件。

---

## 26. 官方资料与实现参考

以下资料用于核对技术能力和边界，实际开发时应固定依赖版本并阅读对应版本文档：

- FastAPI 官方文档：https://fastapi.tiangolo.com/
- React 官方文档：https://react.dev/
- Vite 官方文档：https://vite.dev/
- LangGraph Persistence：https://docs.langchain.com/oss/python/langgraph/persistence
- LangGraph Interrupts：https://docs.langchain.com/oss/python/langgraph/interrupts
- MCP Server Primitives：https://modelcontextprotocol.io/specification/2025-06-18/server/index
- Qdrant 本地启动：https://qdrant.tech/documentation/quick-start/
- Qdrant Hybrid Search：https://qdrant.tech/documentation/search/text-search/hybrid-search/
- Qdrant Hybrid Query：https://qdrant.tech/documentation/search/hybrid-queries/
- Milvus 部署选项：https://milvus.io/docs/install-overview.md
- Milvus Lite：https://milvus.io/docs/milvus_lite.md
- Milvus Standalone 要求：https://milvus.io/docs/prerequisite-docker.md
- Milvus BM25：https://milvus.io/docs/bm25-function.md
- pgvector：https://github.com/pgvector/pgvector
- Ragas Metrics：https://docs.ragas.io/en/stable/concepts/metrics/

---

## 27. 最终决策摘要

本项目第一版采用以下路线：

1. 使用 Python 3.12 开发全部后端、Agent、RAG、Worker 和评测能力。
2. 使用 React + TypeScript + Vite 开发可视化工作台。
3. 使用 FastAPI 构建异步 API，通过 SSE 推送 Agent 事件。
4. 使用 LangGraph 构建受控状态图，不使用自由多 Agent 群。
5. 在 LangGraph 外实现 Agent Harness，负责 Skill、Tool、Policy、Context、Trace 和 Evaluation。
6. 使用 PostgreSQL 作为业务和文档事实来源。
7. 使用 Qdrant 完成 Dense/Sparse 混合检索，并保留迁移 Milvus 的 Port。
8. 使用 Redis + Celery 处理文档解析、Embedding 和评测等长任务。
9. 使用 MinIO 保存原始文件。
10. 使用 MCP 接入 GitHub、日历和受控文件资源，不包装所有内部函数。
11. 使用版本化 Skill 描述领域 SOP、工具白名单、输出 Schema 和评测用例。
12. 所有关键结论提供引用，所有外部写操作经过用户审批。
13. 用固定评测集证明检索、Agent 和工具调用效果。

这套架构足以体现 Python 后端、React 前端、RAG、Agent、MCP、Skill、Harness、数据工程、测试和可观测能力，同时仍保持一个人可以完成和维护的复杂度。
